import About from "@/components/About";

export const metadata = { title: "About — Brian Whirlow" };

export default function AboutPage() {
  return (
    <main className="pt-16">
      <About />
    </main>
  );
}
