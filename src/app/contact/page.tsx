import Contact from "@/components/Contact";

export const metadata = { title: "Contact — Brian Whirlow" };

export default function ContactPage() {
  return (
    <main className="pt-16">
      <Contact />
    </main>
  );
}
