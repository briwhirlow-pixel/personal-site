import Portfolio from "@/components/Portfolio";

export const metadata = { title: "Work — Brian Whirlow" };

export default function WorkPage() {
  return (
    <main className="pt-16">
      <Portfolio />
    </main>
  );
}
